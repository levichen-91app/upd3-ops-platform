# Environment Configuration
NODE_ENV=development
PORT=3000
MARKET=TW

# API Configuration
API_PREFIX=api
API_VERSION=v1
CORS_ENABLED=true

# External API Configuration
# Override whale API URL if needed (optional)
# WHALE_API_URL_OVERRIDE=http://custom-whale-api.example.com/admin
WHALE_API_TIMEOUT=10000
WHALE_API_RETRIES=3

# Database Configuration (for future use)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=upd3_ops
# DATABASE_USER=postgres
# DATABASE_PASSWORD=password

# Security Configuration (for future use)
# JWT_SECRET=your-jwt-secret-key
# JWT_EXPIRES_IN=1h

# Logging Configuration
LOG_LEVEL=info